# Ignore file for helm-docs (https://github.com/norwoodj/helm-docs).
#
# Charts listed here are not yet onboarded to helm-docs. helm-docs falls back to
# its own built-in template for any chart that has no README.md.gotmpl, which
# would overwrite these charts' hand-written READMEs.
#
# To onboard a chart: add charts/<chart>/README.md.gotmpl, document its values
# with `# --` comments, remove its line below, and run `make generate-docs`.
opentelemetry-demo/
opentelemetry-ebpf/
opentelemetry-ebpf-instrumentation/
opentelemetry-kube-stack/
opentelemetry-operator/
opentelemetry-target-allocator/
