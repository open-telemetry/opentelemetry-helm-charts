apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otel.fullname" . }}-test-connection"
  labels:
    {{- include "otel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
