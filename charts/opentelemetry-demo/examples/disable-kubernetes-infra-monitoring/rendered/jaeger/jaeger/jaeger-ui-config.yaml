---
# Source: opentelemetry-demo/charts/jaeger/templates/jaeger/jaeger-ui-config.yaml
# Generates a config map from a file provided by user via `--set-file uiconfig=`
apiVersion: v1
kind: ConfigMap
metadata:
  name: ui-config
  namespace: default
  labels:
    helm.sh/chart: jaeger-4.11.1
    app.kubernetes.io/name: jaeger
    app.kubernetes.io/instance: example
    app.kubernetes.io/version: "2.19.0"
    app.kubernetes.io/managed-by: Helm
data:
  ui-config.json: "{\"useOpenTelemetryTerms\":true}"
