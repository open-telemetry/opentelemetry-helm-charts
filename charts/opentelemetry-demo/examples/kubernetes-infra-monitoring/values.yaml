opentelemetry-kube-stack:
  collectors:
    daemon:
      presets:
        logsCollection:
          enabled: true  # TODO disable when running on Docker Desktop
          storeCheckpoints: true  # TODO disable when running on Docker Desktop
        hostMetrics:
          enabled: true  # TODO disable when running on Docker Desktop
        kubeletMetrics:
          enabled: true
        kubernetesAttributes:
          enabled: true
        kubernetesEvents:
          enabled: true
        clusterMetrics:
          enabled: true
