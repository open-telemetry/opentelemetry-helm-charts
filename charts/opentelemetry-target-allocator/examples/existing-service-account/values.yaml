targetAllocator:
  serviceAccount:
    create: false
    name: "my-service-account"
  config:
    collector_selector:
      namespace: default
      matchlabels:
        app.kubernetes.io/component: agent-standalone
