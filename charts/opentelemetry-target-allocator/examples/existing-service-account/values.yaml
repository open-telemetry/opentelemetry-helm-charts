targetAllocator:
  serviceAccount:
    create: false
    name: "my-service-account"
  config:
    collector_namespace: default
    collector_selector:
      matchlabels:
        app.kubernetes.io/component: agent-standalone
