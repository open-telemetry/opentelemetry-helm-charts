mode: deployment

image:
  repository: "otel/opentelemetry-collector-k8s"

command:
  name: "otelcol-k8s"

configMap:
  create: false
  existingName: "opentelemetry-collector-external"
  configFile: "configmap-external.yaml"

replicaCount: 3

resources:
  limits:
    cpu: 2
    memory: 4Gi
