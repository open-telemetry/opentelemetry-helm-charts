mode: daemonset
config:
  receivers:
    jaeger: null
    otlp: null
    hostmetrics:
      scrapers:
        cpu:
        disk:
        filesystem:
  service:
    pipelines:
      metrics:
        receivers:
          - prometheus
          - hostmetrics
