standaloneCollector:
  enabled: true
  annotations:
    ignored: This is ignored when .annotations is set

agentCollector:
  enabled: false

annotations:
  test: |
    annotations contain metadata that in some cases can be very long and
    developers might chose to break them into multiple lines.
