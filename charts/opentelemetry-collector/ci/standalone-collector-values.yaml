standaloneCollector:
  enabled: true
  annotations:
    test2: both sets of annotations will be added but not deduplicated

agentCollector:
  enabled: false

annotations:
  test: |
    annotations contain metadata that in some cases can be very long and
    developers might chose to break them into multiple lines.
