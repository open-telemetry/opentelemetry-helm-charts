{{- if and .Values.networkExplorer.enabled .Values.networkExplorer.kernelCollector.enabled .Values.networkExplorer.kernelCollector.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "opentelemetry-collector-kernel-collector.serviceAccountName" . }}
  labels:
    {{- include "opentelemetry-collector.labels" . | nindent 4 }}
{{- end -}}
